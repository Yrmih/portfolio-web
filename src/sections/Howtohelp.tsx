"use client";

export default function Howtohelp({ id }: { id: string }) {
  return (
    <section
      id={id}
      className="py-20 bg-[#0b0c14] text-gray-200 relative overflow-hidden"
    >
      <div className="max-w-6xl mx-auto px-6 grid md:grid-cols-2 gap-12 items-center"></div>
    </section>
  );
}
